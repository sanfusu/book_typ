= 代数

$n$ 次实系数多项式一定可以分解成一次因式和二次不可约因式的的乘积。

本原多项式 $f(x) := sum_(i=0)^n a_i x^i$， 其中 $gcd(a_0, a_1, ..., a_n) = 1$。
本原多项式与是否可约无关，$x^2 - 1$ 也是本原多项式。

=== n 次单位根

设 $n >= 1$ 是整数，若复数 $zeta^n = 1$，则 $zeta$ 称为 $n$ 次单位根

=== 本原单位根

$zeta$ 是一个 $n$ 次单位根，若 $n$ 是满足 $zeta^n == 1$ 的最小正整数，则说 $zeta$ 是一个 $n$ 次本原单位根。


== rank 定义

1. $m x n$ 矩阵
2. nihao